<Window x:Class="websocket.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:websocket"
        mc:Ignorable="d" Loaded="Window_Loaded"
        Title="MSDN サーバー" Height="350" Width="525">
    <DockPanel>
        <DockPanel DockPanel.Dock="Top">
            <Button DockPanel.Dock="Right" Click="sendButtonCliek">送信</Button>
            <Button DockPanel.Dock="Right" Click="logClearButtonCliek">ログのクリア</Button>
            <TextBox x:Name="sendText">こちらサーバー</TextBox>
        </DockPanel>
        <TextBox IsReadOnly="True" x:Name="logText" VerticalScrollBarVisibility="Visible" TextWrapping="Wrap">
        </TextBox>
    </DockPanel>
</Window>
